<form method="post" action="{{ path('admin_boutique_e_commerce_delete', {'id': boutique_e_commerce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ boutique_e_commerce.id) }}">
    <button class="btn">Delete</button>
</form>
